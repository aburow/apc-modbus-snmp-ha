---
minimum_pre_commit_version: "3.5.0"

exclude: >-
  (?x)^(
    \.cache/|
    \.pytest_cache/|
    \.ruff_cache/|
    \.venv/|
    node_modules/|
    logs/|
    localonly/|
    __pycache__/|
    .*\.log$
  )

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: mixed-line-ending
        args: ["--fix=lf"]
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=1024"]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.4
    hooks:
      - id: ruff
        entry: scripts/run_uv_tool.sh ruff check
        language: system
      - id: ruff-format
        entry: scripts/run_uv_tool.sh ruff format
        language: system

  - repo: local
    hooks:
      - id: grain-lint
        name: grain check
        entry: scripts/run_uv_tool.sh grain check
        language: system
        types: [python]

      - id: pytest
        name: pytest tests/ -q --tb=short
        entry: scripts/run_uv_tool.sh pytest tests/ -q --tb=short
        language: system
        pass_filenames: false
        files: ^(custom_components/|tests/|scripts/).*\.(py|pyi)$

      - id: shellcheck
        name: shellcheck
        entry: shellcheck
        language: system
        types: [shell]
        stages: [manual]

      - id: shfmt
        name: shfmt
        entry: shfmt -w -i 2 -ci
        language: system
        types: [shell]
        stages: [manual]
