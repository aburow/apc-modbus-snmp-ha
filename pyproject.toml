[project]
name = "apc-modbus-ha"
version = "1.2.3"
requires-python = ">=3.13"

[dependency-groups]
lint = [
  "grain-lint==0.2.0",
  "pre-commit>=4.5.0,<5",
  "pytest>=8,<9",
  "ruff==0.15.4",
  "semgrep==1.156.0",
  "sqlfluff==4.2.0",
  "yamllint==1.37.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
