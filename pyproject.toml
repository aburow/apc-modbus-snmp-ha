[project]
name = "apc-modbus-ha"
version = "2.0.0-dev.5"
requires-python = ">=3.13"

[dependency-groups]
lint = [
  "grain-lint==0.3.1",
  "pre-commit>=4.5.0,<5",
  "pytest>=8,<9",
  "ruff==0.15.15",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
