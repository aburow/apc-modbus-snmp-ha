[project]
name = "apc-modbus-ha"
version = "1.2.6-dev.13"
requires-python = ">=3.13"

[dependency-groups]
lint = [
  "grain-lint==0.3.1",
  "pre-commit>=4.5.0,<5",
  "pytest>=8,<9",
  "ruff==0.15.15",
  "semgrep==1.164.0",
  "sqlfluff>=4.2.0",
  "yamllint==1.38.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
